"""Project description variables, as `kapitan.version` has them. `VERSION`
is krab's version (the host sets it when the evaluator starts)."""

PROJECT_NAME = "kapitan"
DESCRIPTION = "Generic templated configuration management for Kubernetes, Terraform and other things"
URL = "https://github.com/kapicorp/krab"
VERSION = "krab"
